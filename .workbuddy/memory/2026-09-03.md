# 2026-09-03 工作日志

## 学习资料：Agent Skill（智能体技能）
- 交付文件：`docs/agent-skills-learning-guide.html`（自包含 HTML，无外部依赖，浅色极简设计、单一强调色、无图标）。
- 内容依据权威出处整理：Anthropic 官方发布/工程博客、Claude Docs（含中文）、开放标准 skill.md、OpenAI Skills 官方指南与 Cookbook。
- 结构：07 节正文（为什么要学 / 文件格式 / 渐进式披露三层 / 四大特性 / Prompt·Tool·MCP·Skill 辨析 / 最佳实践与安全 / 开放标准生态）+ 分级自测。
- 自测：12 题分三级（概念 4 → 辨析 4 → 应用场景 4），含单选/多选/判断题，每题有解析；支持即时判分、查看参考答案（不计分）、分级与总分统计、重置。
- 交互修复记录：首版选项漏写 input 控件；补 input + value 字母注入 + 判断题显示文案 + peek 中性提示框。JS 语法与结构经 node 校验通过。

## 备注
- 该仓库 memory 目录此前不存在，本次初始化创建。

## 改版 v2（同日，用户反馈后）
- 用户反馈：上一版文字过多；要求「五岁小朋友也能懂」= 图形化直观讲解、能省文字就省；不要漫画风；展示资料多少/权威性是不必要的。
- v2 定位：知识区改为 **6 幅自绘 SVG 图解**（聪明≠会干活 / Skill 文件夹结构 / 渐进式披露三层 / 一次干活四步 / Prompt·Tool·MCP·Skill 分工 / 开放复用与安全检查），每节一句标题+一句图注，无 TOC、无参考清单长列表。
- 风格：严肃几何图示（无脸、无卡通），中性灰+单一克制蓝，问答保留；12 题分级测试保留，但解析压成「一句话」。
- 偏好沉淀：宏哥做学习类 HTML 的原则 = 图形优先、文字极少、严肃不幼稚、不展示资料权威性。写入 MEMORY.md（项目级）。

## 沉淀 Skill：visual-lesson-builder（同日）
- 用户把本次对话复用为 Skill；明确要求：先命名 → 项目级（仅当前仓库，`.workbuddy/skills/`），勿全局。
- 名称：`visual-lesson-builder`（经 AskUserQuestion 确认）；位于 `C:\Users\yangjh\Desktop\repos\bigdata-ai-assignments\.workbuddy\skills\visual-lesson-builder\`。
- 结构：SKILL.md（铁律+工作流+自查清单）+ assets/starter-template.html（可复制起步骨架：CSS 变量/场景版式/「一题一题出」的测验引擎与成绩逻辑，无需从零写 JS）。
- 已用 skill-creator 的 quick_validate.py 校验通过；模板内嵌 JS 经 node 语法检查通过。
- 用户新偏好（待办对齐）：①测验题不要一次全铺开，要一道一道线性出现；②排版交付前应截图自查。这两点已写进 SKILL.md 铁律；`docs/agent-skills-learning-guide.html` 尚待按此返工（浏览器自动化 agent-browser 在本机启动异常 SIGTERM，截图自查未跑通）。
- 环境备注：本机已装 agent-browser（含 Chrome 152），但守护进程启动被终止，疑似沙箱限制 GUI/子进程，后续需换非沙箱或调 troubleshooting。

## 返工测验交互（同日，按用户要求）
- 用户要求：测验题不要一次全部铺开；一道一道线性出现。
- 改动：`docs/agent-skills-learning-guide.html` 增加 `.level{display:none}` + `.level.on{display:block}`、`.q{display:none}` + `.q.active{display:block}`；将脚本替换为基于索引的逐题控制器（`.qnav` 包含"重新开始 / 题目 N/12 / 上一题 / 下一题"）。完成全部作答后成绩卡 `.summary.show` 自动展开并滚到视口。`#quiz` 同名锚点、`summary` 默认隐藏、`reset` 旧按钮已删，事件改绑 `#qreset`。
- 自检：JS 语法 node 通过；DOM dump 显示首题 `class="q active"`、首级 `class="level on"`，证明一次只渲染一道。
- 排版自查：用 `chrome --headless --screenshot` 截全屏，结论：知识区 6 幅图排版无溢出/重叠。测验区位于页面尾部，headless 截图无法滚动到中段（`scrollTo` 在 `--screenshot` 模式下被忽略），改用 `--dump-dom` 与 `--window-size=5200` 验证布局：active 类正确、其它 .q/.level 隐藏。
- 环境备注追加：`chrome.exe` 直接截图可行（已在 `.agent-browser/browsers/chrome-152.0.7977.75/chrome.exe`）；`agent-browser` 守护进程在本沙箱中仍被 SIGTERM。后续若要做"滚到测验区"的中段截图，需改用 CDP 或 Puppeteer。
