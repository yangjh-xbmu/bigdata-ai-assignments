# 2026-09-24

## Python 运算符学习资料（01b-operators.html）

**做了什么**

- 新增 `docs/python-basics/01b-operators.html`：运算符专题图解资料，沿用 01–03 的模板（中性灰 + 靛蓝 `#1d4e89` 单一强调色、手写 SVG、一题一题线性测验引擎）。
- 9 节知识区：①按操作数分五类 ②`/` 与 `//`（含负数向下取整）③`%` 取余与判奇偶 ④`**` 幂 + `*`/`+` 在 str/list 上的第二含义 ⑤`=` 与 `==` ⑥`in`/`is` ⑦`and/or/not` 真值表 ⑧优先级 ⑨实战：秒数换算时分秒。
- 12 道三级测验（概念/辨析/应用 各 4 题），`node --check` 通过。
- **补建 `docs/python-basics/index.html`**：01–03 里一直存在「返回学习资料目录」链接，但该文件从未创建过，属长期死链。现已补上，含已完成/待制作状态标注。
- 更新 `docs/learning-map.md`：新增 U1b 运算符行、说明其插入理由、附录「资料完成情况」表。

**顺带修的既有 bug**

- 01/02/03 三份资料的测验选项排版缺陷：`.ltr` 只给 `min-width`，遇到 `<code>` 紧跟时字母与内容贴合（渲染成 `A3` 而非 `A 3`）。已给三份文件统一加 `.opts .ltr{display:inline-block;width:18px}` 与 `.opts label code{margin-left:3px}`。

**编号决策**

运算符用 `01b` 插在 01 与 02 之间，而非整体重编号。理由：重编号要改动 02–08 的文件名、learning-map 引用，以及 5 份文件里的页脚互链，成本高且会破坏外部已有链接。01b 语义上也说得通（U1 之后、U2 之前的延伸专题）。

**遗留**

- 04–08 五份资料仍未制作（字典·集合·推导式 / 函数 / 模块标准库pip / 文件读写异常 / 类与对象调试）。
- 工作区有两处无关的未提交改动，非本次产出：`.workbuddy/skills/visual-lesson-builder/`（SKILL.md 与 assets/starter-template.html 显示为已删除，但目录已不存在——疑似此前被移动或清理）、`scripts/02.py`（未跟踪）。

**atomgit 尝试与回退（10:15）**

- 需求链条：设项目级身份 → 推 atomgit → 推 GitHub。**最终 atomgit 远端的添加已删除**（`git remote remove atomgit`），本仓库只保留 GitHub origin。
- 关键事实：**atomgit.com 是 GitCode 的镜像域**，真实克隆地址在 `gitcode.com`。仓库真实地址 `https://gitcode.com/gcw_LeQgrjfy/bigdata-ai-assignments2.git`（用户名是 `gcw_LeQgrjfy`——带 jf，不是截图里姓名显示的 `gcw_LeQgrfjy`；用错名字会 404）。
- **GitCode 已禁用密码认证**：推送直接报 `HTTP Basic: Access denied. The password-based authentication of Git has been removed. Please use your personal access token`。凭据管理器里 `Windows2026` 是登录密码，**只能读不能写**。要推 GitCode 必须去平台生成 PAT。
- **项目级身份已恢复为 GitHub 主账号** `yangjh-xbmu / yangjh@xbmu.edu.cn`（此前为推送 atomgit 临时设成了 `gcw_LeQgrfjy`）。教训：临时的项目级身份用完要记得还原，否则后续 GitHub 提交会记错人。
- 推送结果：commit `6e2c5f5` 已上 GitHub，`ls-remote` 核验远端 main 与本地 HEAD 哈希一致。
- 网络观测：此次推送走**会话注入代理**一次成功（此前同一代理报 502）。GitHub 在此环境是**间歇性可达**，重试往往就通，不必急着上复杂降级方案。

**本仓库曾新增项目级 git 身份（10:07，已于 10:15 还原）**

- 截图要求的那两条设置，**按项目级写入**（`git config --local`），未加 `--global`（那条会改掉用户的 GitHub 主账号身份 yangjh-xbmu，跨所有仓库生效，风险不对称）。
- 写入结果：`user.name=gcw_LeQgrfjy`、`user.email=arSFdHGH8gOgwBIr.gcw_LeQgrfjy@noreply.gitcode.com`；`git var GIT_AUTHOR_IDENT` 已校验生效。
- 仓库内自测：`ls-remote origin` 正常（说明仓库网络可达），但 GitHub 整体时通时不通——会话注入代理报 `CONNECT tunnel failed 502`，本机 10808 端口在听但 TLS 握手失败，清代理直连超时。
- **指向 atomgit 的远端未配置**：`https://atomgit.com/gcw_LeQgrfjy/bigdata-ai-assignments2.git` 匿名访问返 403「project could not be found」（仓库名与实际 UI 显示的 `bigdata-ai-assignments2` 不一致，或需登录凭据）。等用户确认 URL 与凭据后再加 remote，不擅自改 origin。

**环境备注**

- 本机 chrome 位于 `~/.agent-browser/browsers/chrome-153.0.8010.36/chrome.exe`，可用 `--headless=new --screenshot` 做排版自查。
- 坑：Chrome 每次换新 `--user-data-dir` 会挂起（超时不出图），**复用同一个 profile 目录**即可稳定。截图分段核对时用注入 `<style>html{margin-top:-Npx}</style>` 的方式逐屏拍摄效果最好。
